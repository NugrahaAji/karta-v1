/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tambahkan domain gambar jika pakai next/image
  images: {
    domains: ["avatars.githubusercontent.com"],
  },
};

export default nextConfig;
